name: Checks
on: [push, pull_request]
env:
  POSTGIS_VERSION: 3.6.2
jobs:
  linux:
    name: PostgreSQL ${{ matrix.postgres }} + PostGIS 3.6.2 (Linux)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        postgres: ["18.3", "17.9", "16.13"]
    steps:
      - name: Checkout project
        uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
      - name: Test PostgreSQL bundle
        run: ./gradlew :debian-platforms:testAmd64DebianJar -Pversion=${{ matrix.postgres }}-TEST -PpgVersion=${{ matrix.postgres }} -PpostgisVersion=$POSTGIS_VERSION
        timeout-minutes: 10
  alpine-linux:
    name: PostgreSQL ${{ matrix.postgres }} + PostGIS 3.6.2 (Alpine Linux)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        postgres: ["18.3", "17.9", "16.13"]
    steps:
      - name: Checkout project
        uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
      - name: Test PostgreSQL bundle
        run: ./gradlew :alpine-platforms:testAmd64AlpineJar -Pversion=${{ matrix.postgres }}-TEST -PpgVersion=${{ matrix.postgres }} -PpostgisVersion=$POSTGIS_VERSION
        timeout-minutes: 10
  macos:
    name: PostgreSQL ${{ matrix.postgres }} + PostGIS 3.6.2 (MacOS arm64)
    runs-on: macos-14
    strategy:
      fail-fast: false
      matrix:
        postgres: ["18.3", "17.9", "16.13"]
    steps:
      - name: Checkout project
        uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
      - name: Install psql executables
        run: |
          brew update
          brew install postgresql
          echo "$(brew --prefix postgresql)/bin" >> $GITHUB_PATH
      - name: Test PostgreSQL/PostGIS bundle
        run: ./gradlew :custom-darwin-platform:testCustomDarwinJar -Pversion=${{ matrix.postgres }}.0-TEST -PpgVersion=${{ matrix.postgres }} -PpostgisVersion=$POSTGIS_VERSION -PdistName=darwin -ParchName=arm64v8
        timeout-minutes: 45
  windows:
    name: PostgreSQL ${{ matrix.postgres }} + PostGIS 3.6.2 (Windows amd64)
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        postgres: ["18.3", "17.9", "16.13"]
    steps:
      - name: Checkout project
        uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
      - name: Install GNU Wget
        run: choco install wget
      - name: Update system paths
        run: echo $PGBIN >> $GITHUB_PATH
        shell: bash
      - name: Test PostgreSQL/PostGIS bundle
        run: ./gradlew :custom-windows-platform:testCustomWindowsJar -Pversion=${{ matrix.postgres }}.0-TEST -PpgVersion=${{ matrix.postgres }} -PpgBinVersion=${{ matrix.postgres }}-1 -PpostgisVersion=$POSTGIS_VERSION -PdistName=windows -ParchName=amd64
        shell: bash
        timeout-minutes: 25
  windows-arm64:
    name: PostgreSQL 18.2 + PostGIS 3.6.2 (Windows ARM64)
    runs-on: windows-11-arm
    steps:
      - name: Checkout project
        uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
      - name: Test PostgreSQL/PostGIS bundle
        run: ./gradlew :custom-windows-platform:testCustomWindowsJar -Pversion=18.2.0-TEST -PpgVersion=18.2 -PpostgisVersion=$POSTGIS_VERSION -PdistName=windows -ParchName=arm64v8
        shell: bash
        timeout-minutes: 30
